import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Geliştirme sırasında köşedeki Next.js Dev Tools göstergesini gizle
  devIndicators: false,
};

export default nextConfig;
